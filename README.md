# Reloop

Item swap & exchange marketplace — front-end MVP (mock data, no backend yet).

## Run locally
```
npm install
npm run dev
```

## Build
```
npm run build
```

## Deploy
Push this folder to a GitHub repo, then import it on vercel.com — no config needed,
Vercel auto-detects Vite.
